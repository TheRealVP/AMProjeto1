<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pt.isec.a2004009394.amprojeto1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AMProjeto1">
        <activity android:name=".escolher_lista"></activity>
        <activity android:name=".NovaLista" />
        <activity android:name=".EliminarItem" />
        <activity android:name=".AdicionarItem" />
        <activity android:name=".EditarItem" />
        <activity android:name=".GerirItems" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>